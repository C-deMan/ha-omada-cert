name: "Omada & Cloudflare SSL Certificate Manager"
version: "1.0.0"
slug: "omada_cloudflare_ssl"
description: "Automatically issue and renew Let's Encrypt certificates via Cloudflare DNS-01 and deploy them to TP-Link Omada Controller and Home Assistant."
url: "https://github.com/C-deMan/ha-omada-cert"
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
startup: "services"
boot: "auto"
map:
  - "ssl:rw"

options:
  domains:
    - "omada.yourdomain.com"
  cloudflare_api_token: "YOUR_CLOUDFLARE_API_TOKEN"
  letsencrypt_email: "admin@yourdomain.com"
  letsencrypt_staging: false
  renew_interval_hours: 12
  copy_to_ha_ssl: true
  ssl_subdir: "omada"
  omada:
    enabled: true
    url: "https://192.168.1.1:8043"
    username: "admin"
    password: "your_omada_password"
    verify_ssl: false

schema:
  domains:
    - str
  cloudflare_api_token: password
  letsencrypt_email: email
  letsencrypt_staging: bool?
  renew_interval_hours: int(1,720)?
  copy_to_ha_ssl: bool?
  ssl_subdir: str?
  omada:
    enabled: bool
    url: str?
    username: str?
    password: password?
    verify_ssl: bool?
